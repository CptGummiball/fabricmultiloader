plugins {
    base
}

description = "UniversalExampleMod — the reference mod for Minecraft 1.20.1, 1.21–1.21.1 and 1.21.4."

// Placeholder. This module becomes a real FabricMultiLoader mod project in
// implementation step 17, once the Gradle plugin exists. Keeping it in the
// settings file from the start means the module layout of chapter 22.1 is
// visible and CI never has to be restructured later.
